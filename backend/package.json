{
  "name": "chathub-backend",
  "version": "1.0.0",
  "description": "ChatHub backend: WebSocket real-time chat + REST + SQLite",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
